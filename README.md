Happy Headlines

For week 40: Cache Thingy

Two dashboards to monitor cache efficiency:
- **CommentService** -> `/api/CacheStats` (shows CommentCache hit/miss ratio) http://localhost:5001/api/CacheStats
- **ArticleService** -> `/api/CacheDashboard` (shows ArticleCache hit/miss ratio) http://localhost:8080/api/CacheDashboard

Each cache logs its activity (HIT/MISS) 

